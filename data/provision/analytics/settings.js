'use strict';

var settings = [
    {
        type: 'settings',
        name: 'Analytics Settings',
        json: {
            env: 'dashboard',
            enabled: false
        }
    }
];

module.exports = settings;